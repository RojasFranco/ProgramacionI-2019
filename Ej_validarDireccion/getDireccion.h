int getString ( char* msg,
                char* msgError,
                int minimo,
                int maximo,
                int reintentos,
                char* resultado);

int getDireccion(char* msg,
                char* msgError,
                int minimo,
                int maximo,
                int reintentos,
                char* resultado);

int isValidDireccion(char* resultado);
